```mermaid
sequenceDiagram
participant user
participant browser
participant server
  user->browser: Input content & click save button

  browser->browser: Add the new note to notes list.\nRerenders the note list.
  browser->user: Display updated page

  Note right of browser: content-type: application/json

  browser->+server: POST to https://studies.cs.helsinki.fi/exampleapp/new_note_spa
  server-->-browser: Code 201 Created
```